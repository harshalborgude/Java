package org.hrb;
import org.hrb.*;
import org.hrb.Blog.Java;
//import org.hrb.platform.Java;

// It is a way to group many classes belonging to a specific project.A package is like a folder of similar things such as music or a folder of movies.
// The package adds a different level of inheritance, this level of inheritance is by default applied to all the members created in the method.
// We can also import the packages which will result in accessing all the classes and methods in it.
// If you want to use the class of a particular package, you can also use its full name, which includes its complete package hierarchy.

public class Packages {

	public static void main(String[] args) {
		Java blog = new Java();
		blog.usedFor();
	   
 
	}
}
